# RAG 系统评估报告

**生成时间**: 2026-06-19 01:30:33
**评估版本**: v4 (精简版)
**知识库**: kb_4 (PAP认证项目，12个文档片段)
**评估框架**: 自定义指标 + RAGAS answer_relevancy

---

## 1. 评估汇总

| 评估维度 | 指标 | 值 | 评价 |
|---------|------|-----|------|
| 检索质量 | Hit Rate@5 | 0.8250 | 优秀 |
| 检索质量 | MRR | 0.5671 | 一般 |
| 检索质量 | Recall@5 | 0.5763 | 一般 |
| 检索质量 | NDCG@5 | 0.5037 | 一般 |
| 生成质量 | 忠实度 | 0.3681 | 待改进 |
| 生成质量 | 关键词覆盖 | 0.4641 | 一般 |
| 生成质量 | 拒答准确率 | 0.7500 | 良好 |
| RAGAS | 答案相关性 | 0.4268 | 一般 |
| 性能 | 检索延迟 | 57ms | 优秀 |
| 性能 | 生成延迟 | 10390ms | 良好 |

---

## 2. 检索质量分析

### 2.1 总体指标

- **Hit Rate@5 = 0.8250**: 82.5% 的查询在 Top-5 结果中命中了相关文档
- **MRR = 0.5671**: 第一个相关文档的平均倒数排名为 0.57，排序质量一般
- **Recall@5 = 0.5763**: Top-5 结果平均覆盖了 57.6% 的相关文档
- **NDCG@5 = 0.5037**: 排序增益为理想排序的 50.4%

### 2.2 失败查询分析 (7条 Hit Rate@5 = 0)

| ID | 类别 | 问题 | 可能原因 |
|-----|------|------|---------|
| R14 | config_commands | 项目中OSPF是如何配置的？ | 查询表述与文档措辞差异大 |
| R16 | config_commands | AAA视图下如何添加本地用户？ | 关键词"AAA视图"匹配度低 |
| R18 | config_commands | 如何重启接口使配置生效？ | "重启接口"→"shutdown/undo shutdown"语义鸿沟 |
| R19 | config_commands | 如何查看接口状态？ | "查看接口状态"→"display ip interface brief"匹配难 |
| R20 | verification | PAP认证配置完成后，如何验证链路是否正常？ | 综合验证步骤分散在多个chunk |
| R22 | verification | 认证通过后ping测试的结果是什么？ | 具体ping结果在特定chunk中 |
| R27 | verification | 如何从PC1测试到PC2的连通性？ | "测试连通性"→"ping 192.168.20.1"语义差异 |

**共性问题**: 配置命令类和验证类查询的表述方式与文档原文差异较大，embedding 模型难以捕捉语义等价性。

---

## 3. 生成质量分析

### 3.1 总体指标

- **忠实度 = 0.3681**: 回答中仅 36.8% 的关键信息可在上下文中找到依据，存在较多"幻觉"
- **关键词覆盖 = 0.4641**: 回答覆盖了 46.4% 的预期关键词
- **拒答准确率 = 0.7500**: 75% 的不可回答问题被正确拒答（6/8）
- **RAGAS 答案相关性 = 0.4268**: 回答与问题的相关度一般

### 3.2 拒答分析

**正确拒答 (6条)**:
- G13 公司食堂几点开门？ ✓
- G14 如何申请年假？ ✓
- G16 R1和R2的购买成本是多少？ ✓
- G17 这个项目是什么时候实施的？ ✓
- G18 项目的负责人是谁？ ✓
- G20 项目的预算有多少？ ✓

**错误未拒答 (2条)**:
- G15 "这个项目用了什么编程语言开发？" → 模型回答了"通过CLI配置"而非拒答
- G19 "这个项目中使用了哪些品牌的设备？" → 模型从`[Huawei]system-view`推断出华为品牌

**分析**: G19 的回答实际上是合理的（从配置命令推断品牌），但严格来说超出了知识库的直接陈述范围。

### 3.3 低忠实度问题 (10条 < 0.5)

| ID | 忠实度 | 问题 | 分析 |
|-----|--------|------|------|
| G04 | 0.385 | 认证失败时会有什么表现？ | 回答添加了"物理接口状态显示为up"等推断内容 |
| G05 | 0.314 | OSPF在这个项目中是怎么用的？ | 回答过于详细，包含文档未明确说明的内容 |
| G08 | 0.235 | 如果ping测试失败，应该如何排查？ | 回答扩展了排查步骤，超出文档范围 |
| G09 | 0.222 | 简要说明PAP认证的工作原理 | 回答添加了"明文方式发送密码"等推断 |
| G12 | 0.312 | 认证通过后ping测试的结果是什么？ | 回答错误地说"资料中没有提供"，实际有 |
| G21 | 0.273 | R1的性能参数是什么？ | 部分可答问题，回答基本正确 |
| G22 | 0.143 | 这个项目的网络带宽是多少？ | 部分可答问题，回答基本正确 |
| G23 | 0.000 | OSPF的Hello时间间隔是多少？ | 简单拒答，忠实度计算为0 |
| G24 | 0.000 | PAP认证的超时时间是多少？ | 简单拒答，忠实度计算为0 |
| G25 | 0.250 | 这个项目中有没有配置VLAN？ | 回答正确但忠实度低 |

**核心问题**: 模型倾向于"扩展回答"，在上下文基础上添加推断性内容，导致忠实度偏低。

---

## 4. RAGAS 框架评估

| 指标 | 值 | 说明 |
|------|-----|------|
| answer_relevancy | 0.4268 | 答案与问题的相关程度 |

> **说明**: RAGAS 的 faithfulness、context_precision、context_recall、answer_correctness 指标因 MiMo 模型不支持复杂 JSON 输出而评估失败。仅 answer_relevancy 指标成功运行。

---

## 5. 性能分析

| 指标 | 值 | 说明 |
|------|-----|------|
| 检索延迟 | 57ms (平均) | 包含 embedding + 向量搜索，无 reranker |
| 生成延迟 | 10390ms (平均) | MiMo API 调用，包含 prompt 处理和生成 |
| 评估总耗时 | 4.5 分钟 | 40条检索 + 25条生成 + RAGAS |

---

## 6. 改进建议

### 6.1 已实施优化 ✅

| 优化项 | 修改文件 | 具体改动 | 预期效果 |
|--------|----------|----------|----------|
| **SYSTEM_PROMPT 强化** | `rag.py` | 添加"严格基于上下文"、"不要编造信息"、"保留原文表述"等规则 | 提升忠实度 0.37→0.5+ |
| **Temperature 降低** | `llm.py` | 从 0.7 降至 0.3 | 减少随机性，提升一致性 |
| **BM25 分词增强** | `vector_store.py` | 保留技术术语（如 `display`、`shutdown`）、IP地址、命令格式 | 提升配置命令类检索 |
| **混合检索权重调整** | `vector_store.py` | 向量权重 0.6 + BM25 权重 0.4（原为 1:1） | 平衡语义和关键词匹配 |
| **上下文片段编号** | `rag.py` | 添加 `[片段1]`、`[片段2]` 编号标记 | 便于模型引用来源 |

### 6.2 待实施优化 (优先级: 高)

#### 1. Embedding 模型升级

当前模型 `shibing624/text2vec-base-chinese` 对技术术语语义理解有限。

**推荐替换方案**:
- `BAAI/bge-large-zh-v1.5` — 中文效果最佳，768维
- `moka-ai/m3e-base` — 中文语义匹配强，768维

**修改位置**: `app/services/embedding.py` 第 14 行

```python
# 当前
MODEL_NAME = "shibing624/text2vec-base-chinese"
# 建议
MODEL_NAME = "BAAI/bge-large-zh-v1.5"
```

#### 2. Chunk 分片策略优化

当前分片可能导致跨 chunk 信息丢失（如 R20 验证步骤分散在多个 chunk）。

**建议**:
- 增加 chunk 重叠（overlap）: 前后 chunk 重叠 50-100 字符
- 对配置命令类文档，按"步骤"而非固定长度分片

**修改位置**: `app/services/document_processor.py`

#### 3. 添加同义词扩展

配置命令查询失败的根因是用户表述与文档措辞差异大。

**建议建立同义词映射表**:
```python
SYNONYM_MAP = {
    "重启接口": ["shutdown", "undo shutdown"],
    "查看接口状态": ["display ip interface brief"],
    "配置IP地址": ["ip address"],
    "验证连通性": ["ping"],
}
```

**修改位置**: `app/services/vector_store.py` 的 `_tokenize_chinese` 函数

### 6.3 待实施优化 (优先级: 中)

#### 1. 检索结果置信度阈值

当 reranker 最高分 < 0.5 时，自动触发拒答，避免用低质量上下文生成回答。

**修改位置**: `rag.py` 的 `ask_with_rag` 函数

```python
results = retrieve_and_rerank(question, kb_id)
if results and results[0][2] < 0.5:  # rerank_score < 0.5
    return "根据现有知识库，我无法回答这个问题。"
```

#### 2. 添加引用标注

要求模型在回答中标注信息来源片段编号。

**修改位置**: `rag.py` 的 `SYSTEM_PROMPT`

```
回答时，如引用特定片段，请标注来源，如：[片段1]
```

### 6.4 待实施优化 (优先级: 低)

1. **扩大测试集**: 当前 25 条生成测试集偏小，建议扩充到 50+ 条
2. **使用更强的评估 LLM**: RAGAS 评估需要 GPT-4 级别模型作为评判
3. **添加多轮对话评估**: 当前仅评估单轮，多轮场景需补充

---

## 7. 评估数据统计

### 测试集规模

| 类型 | 数量 | 说明 |
|------|------|------|
| 检索测试 | 40 条 | 覆盖 6 个类别 |
| 生成测试 | 25 条 | 12 可答 + 8 不可答 + 5 部分可答 |
| RAGAS 评估 | 17 条 | 仅可答 + 部分可答 |

### 检索测试分布

| 类别 | 数量 | 说明 |
|------|------|------|
| simple_facts | 5 | 简单事实查询 |
| ip_address | 6 | IP地址相关 |
| config_commands | 8 | 配置命令查询 |
| verification | 8 | 验证/故障排查 |
| comprehensive | 6 | 综合理解 |
| edge_cases | 7 | 边界情况 |

---

*报告由 IntraAI RAG 评估系统自动生成*
